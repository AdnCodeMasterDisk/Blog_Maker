{
  "name": "tailwind-builder",
  "version": "1.0.0",
  "description": "this is a builder based in tailwind css",
  "main": "lib/main.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "build": "browserify lib/main.js -o js/bundle.js",
    "cm": "node-sass sass/app.sass -o css &&  browserify lib/main.js -o js/bundle.js"
  },
  "author": "caustic code",
  "license": "ISC",
  "dependencies": {
    "browserify": "^16.5.1",
    "dragula": "^3.7.2",
    "jquery": "^3.5.1",
    "js-file-download": "^0.4.12",
    "node-sass": "^5.0.0"
  }
}
